<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jjh.st.member.STMemberDao">
 <insert id="createOne" parameterType="org.jjh.st.member.STMemberDto">
  insert into st_member
  values(#{id}, #{pass }, #{gender }, #{name }, st_member_seq.nextval, #{birth }, #{connect }, #{email }, 
  #{address }, #{file }, #{school }, #{cost }, #{subject }, #{day }, sysdate, sysdate, #{state })
 </insert>
 <select id="readOne" resultType="org.jjh.st.member.STMemberDto">
  select * from st_member where num=#{num}
 </select>
  <select id="readAll" resultType="org.jjh.st.member.STMemberDto">
  select * from st_member order by joinDate desc
 </select>
</mapper>
